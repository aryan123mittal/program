# Count Squares in python


import math
def count(n):
    if n == 1:
        return 1
    if n < 1:
        return 0
    a = int(math.sqrt(n-1))
    return a
n = 17
print("The number of perect squares is",count(n))  


# time complexity is O(1)
# space complexity is O(1)
